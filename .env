# Local environment variables for JSFormhandler
# For local development set DEV_URL to the path where your site is served
DEV_URL=http://localhost:5500/Tconcepts

# Live production URL
LIVE_URL=https://tconcepts.be

# Optional: override the base URL entirely
# BASE_URL=

# Optional: full redirect URL after form submission (overrides BASE_URL)
# REDIRECT_URL=
