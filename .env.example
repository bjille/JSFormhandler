# .env.example - copy to .env for local overrides
# URL used during local development (your live server may host the site under a path)
DEV_URL=http://localhost:5500/Tconcepts

# Live production URL
LIVE_URL=https://tconcepts.be

# Optional explicit base URL (overrides NODE_ENV logic)
# BASE_URL=

# Optional explicit redirect URL after form submission (overrides BASE_URL)
# REDIRECT_URL=
